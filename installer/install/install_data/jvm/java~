#!/bin/sh
ln -s /mnt/user/jvm/cbc/cbc ./
if [ -d /mnt/user/jvm/scala ];
then
	ln -s /mnt/user/jvm/scala ./
fi
LD_LIBRARY_PATH=/mnt/user/lib /mnt/user/jvm/jamvm -mx8M -Xbootclasspath:/mnt/user/lib/classes.zip:/mnt/user/lib/glibj.zip $@ ./
rm ./cbc
if [ -d ./scala ];
then
	rm ./scala
fi
